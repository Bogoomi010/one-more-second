# Game Planning Document

## 현재 게임 분석
- 장르: 탄막 회피 기반 아케이드 생존 게임 (1인, 세션형)
- 핵심 루프:
  1. Enter로 게임 시작
  2. 방향키로 플레이어 이동하며 탄환 회피
  3. 시간 경과에 따라 탄환 생성 주기 단축(난이도 상승)
  4. 피격 시 목숨 감소, 0이 되면 게임 종료
  5. 생존 시간 기반 점수/코인 획득 후 랭킹 반영
- 현재 재미 요소:
  - 즉시성 높은 도전 재미(회피 컨트롤, 반응속도)
  - 기록 갱신/랭킹 경쟁 재미(글로벌/국가/일일)
  - 누적 성장(코인, 스킨, 업적, 데일리 챌린지)
  - 짧은 런 반복 플레이에 적합한 구조
- 문제점:
  - 플레이 스타일 선택지가 적어 회차 간 체감 변화가 제한적임
  - 스킨은 존재하지만 "게임 규칙 선택"과 연결되지 않아 전략 분기가 약함
  - 랭킹 경쟁 중심 게임인데, 고수와 초보가 모두 만족할 난이도 조절 수단이 부족함

## 재미 확장 전략

### 1) 기믹 ON/OFF 로드아웃 시스템 (핵심)
- 설명: 플레이 시작 전 기믹을 켜고/끄는 `Loadout` 화면 제공. ON한 기믹 조합으로 런을 시작.
- 재미 요소 강화 방식: 같은 게임이라도 선택한 기믹에 따라 난이도와 플레이 감각이 달라짐.
- 사용자 감정: 직접 세팅을 만든다는 주도감, 조합 최적화 재미.
- 차별화 포인트: 탄막 회피 + 규칙 커스터마이징 결합.
- 개발 난이도: M
- 기술적 영향:
  - `gameSystem/types.ts`에 `GameplayModifiers` 타입 추가
  - `gameSystem/modifiers.ts`에 기믹 레지스트리(정의/가중치/스폰 훅) 추가
  - `settings/storage`에 modifier 저장/로드 추가
  - `GamePanel` 시작 UI에 토글 목록 추가
  - `GameCanvas` 업데이트 루프에 modifier 적용
- 밸런스 리스크: 쉬운 조합으로 기록 인플레이션 발생 가능.
- 부작용: 룰이 많아지면 신규 유저가 어려워할 수 있음.
- 도입 전략: 3개 기믹으로 시작, 점진적으로 확장.

### 2) 단일 랭킹 + 기믹 가중치 점수 시스템
- 설명: 랭킹은 하나만 운영하고, 기믹 ON 시 기본 점수에 가중치를 더하는 방식으로 운영.
- 점수 규칙:
  - `baseScore = survivalSeconds`
  - `adjustmentScore = Σ(baseScore × gimmickWeight)`
  - `finalScore = baseScore + adjustmentScore`
  - 가중치는 합연산으로 계산하며 곱연산은 사용하지 않음
  - 시즌 중 기믹 가중치(`weight`)는 변경하지 않음
  - 가중치 상한선은 초기에는 두지 않음
  - 소수점 점수는 `Math.floor`로 정수화하여 저장
- 재미 요소 강화 방식: 공정한 단일 경쟁축을 유지하면서도 기믹 선택 동기를 제공.
- 사용자 감정: "기록이 인정받는다"는 신뢰감 + 점수 산식 이해 가능성.
- 차별화 포인트: 커스텀 규칙을 별도 브라켓 없이 점수 설계로 흡수.
- 개발 난이도: M
- 기술적 영향:
  - `ranking.ts`에 점수 세부 항목(`baseScore`, `adjustmentScore`, `usedGimmicks`) 저장
  - 스코어 제출 시 적용 기믹 목록, 각 기믹 가중치 스냅샷, 가중치 합 기록
  - 랭킹 UI에 점수 상세 표시 추가
- 밸런스 리스크: 특정 가중치 조합이 메타를 고착시킬 가능성.
- 부작용: 점수 계산 방식이 길어지면 초반 이해 비용 증가.
- 도입 전략: 초기 3개 기믹 기준으로 시작 후, 시즌 단위로만 가중치 재설계.

### 3) 스킨 시스템의 게임 코드 일원화
- 설명: 현재 색상 중심 스킨을 `visual + optional trait` 구조로 확장.
- 재미 요소 강화 방식: 스킨 선택이 단순 외형이 아닌 플레이 정체성 선택으로 연결됨.
- 사용자 감정: 소유/표현 욕구 + 빌드 시너지 탐색 재미.
- 차별화 포인트: 스킨을 단순 상점 아이템이 아닌 런 준비 요소로 승격.
- 개발 난이도: M
- 기술적 영향:
  - `skins.ts`에 `effects` 또는 `tags` 추가
  - `getSkin()` 결과를 `GameCanvas` modifier 계산에 포함
  - 상점/장착 UI에 효과 설명 추가
- 밸런스 리스크: 특정 스킨이 사실상 필수 선택지가 될 위험.
- 부작용: P2W 오해 가능성.
- 도입 전략: 랭킹 영향 없는 trait부터 시작 후 제한적 적용.

### 4) 짧은 런 메타 고정 (제품 원칙)
- 설명: 평균 30~90초 런을 유지하도록 난이도 곡선과 보상 루프를 고정 설계.
- 재미 요소 강화 방식: 빠른 재도전과 기록 갱신 사이클을 유지해 중독성 강화.
- 사용자 감정: "한 판만 더" 흐름의 몰입.
- 차별화 포인트: 긴 파밍보다 고밀도 세션 기반 경쟁에 집중.
- 개발 난이도: S
- 기술적 영향:
  - 스폰 간격 하한/상한 재조정
  - 보상 계산은 러닝타임보다 `최고기록` 중심으로 보정
  - 게임오버 후 재시작 플로우 1~2클릭 이내 유지
- 밸런스 리스크: 너무 짧으면 운 요소가 커지고, 너무 길면 반복성이 약해짐.
- 부작용: 신규 콘텐츠 부족 시 빠른 피로 가능.
- 도입 전략: 목표 런타임 KPI(중앙값 45초 전후)로 라이브 튜닝.

### 5) 기믹 후보(1차)
- 설명: 초기 ON/OFF 기믹은 아래 3개로 제한. 모든 기믹 탄환 스킨은 `icon-bullet-neon-blue.png`를 사용.
- 재미 요소 강화 방식: 룰 이해 부담을 낮추고 조합 재미를 빠르게 검증.
- 사용자 감정: 명확한 선택과 즉각적인 체감.
- 차별화 포인트: 최소 기능으로도 조합 플레이 가능.
- 개발 난이도: S~M
- 기믹 목록:
  - `Crossline 40/80` (가중치 예시: +12%)
  - 효과: 가로/세로 라인에서 직선 탄환 발사
  - 스폰 규칙:
    - 가로 라인: `y=40`, `y=80`에서 좌->우/우->좌 직선 발사
    - 세로 라인: `x=40`, `x=80`에서 상->하/하->상 직선 발사
    - 기존 추적탄 스폰과 함께 동작 가능하도록 별도 스폰 파이프라인으로 분리
  - `Shrink Field` (가중치 예시: +10%)
  - 효과: 플레이 가능한 게임 영역을 중앙 기준 80%로 축소
  - 적용 규칙:
    - 시작 직후 즉시 축소하지 않고 3초 워밍업 후 적용
    - 축소 영역 밖은 이동 불가(벽 처리), 플레이어/탄환 클램핑
  - `Haste Bullets` (가중치 예시: +8%)
  - 효과: 플레이어 추적 탄환의 속도를 기본 대비 110%로 증가
  - 적용 규칙:
    - 기본 탄속 상수에 승수(`1.1`)를 적용
    - 다른 속도 계열 기믹이 추가되더라도 승수 합성 순서를 고정(시즌 고정 규칙)
- 점수 반영 원칙:
  - 각 기믹은 점수 가중치(%)를 가진다.
  - 최종 점수는 기본 점수 + 기믹 보정 점수(합산)로 계산한다.
  - 기록에는 기믹 ID/이름/가중치 스냅샷을 함께 저장한다.
- 밸런스 리스크: 가중치 설계 실패 시 메타 고착.
- 부작용: 가중치 설명이 불명확하면 불만 증가.
- 도입 전략: 모든 기믹의 점수 가중치/랭킹 반영 규칙을 UI에 명시.

### 6) 점수 제출 팝업/랭킹 표시 원칙
- 랭킹 화면에 다음 정보를 함께 표시:
  - 최종 점수
  - 기본 생존 시간
  - 기믹 보정 점수
  - 사용 기믹 목록
- 표시 예:
  - `135점`
  - `(기본 100초 + 보정 35점)`
  - `기믹: Crossline 40/80(+12%), Shrink Field(+10%), Haste Bullets(+8%)`

### 7) 확장성 설계 원칙 (필수)
- 목표: 신규 기믹 추가 시 `switch` 분기 추가를 최소화하고, 정의 파일 + 훅 구현만으로 확장.
- 구조:
  - `modifiers.ts`에 `ModifierDefinition[]` 레지스트리 유지
  - 각 기믹은 필요한 훅만 구현:
    - `onRunStart(context)`
    - `modifyPlayfield(bounds)`
    - `modifySpawnPlan(plan, state)`
    - `modifyBulletStats(stats)`
    - `onTick(state, dt)`
    - `onRunEnd(result)`
- 실행 방식:
  - 런 시작 시 활성 기믹을 `resolvedModifiers`로 컴파일
  - 게임 루프에서 훅 체인을 순서대로 적용
  - 점수 계산 시 동일한 `resolvedModifiers`를 사용해 저장 데이터와 화면 표시를 일치
- 데이터 호환:
  - `RankingEntry`는 `finalScore`가 없으면 기존 `score`를 fallback 사용
  - `usedGimmicks`가 없는 과거 데이터도 문제 없이 렌더링되도록 처리

## 장르 신선도 확장 아이디어
- 기존 장르 한계:
  - 고득점 경쟁이 실력 단일축으로 수렴해 메타 다양성이 떨어짐
  - 외형 수집과 실제 플레이 전략이 분리되기 쉬움
- 새로운 조합 아이디어:
  - 탄막 생존 + 로드아웃 토글 빌드
  - 탄막 생존 + 스킨 trait 시너지
  - 탄막 생존 + 단일 랭킹 점수 상세 공개
- 현실 적용 가능성:
  - 단기(현실적): 토글 로드아웃 + 단일 랭킹 가중치 시스템
  - 중기(투자 필요): 스킨 trait, 기믹 수 확장
  - 장기(실험적): 시즌 메타/이벤트 룰셋

## 장기 비전
- 3개월 방향
  - `기믹 ON/OFF + 단일 랭킹 가중치` MVP 출시
  - 짧은 런 KPI 고정: 중앙값 런타임 45초, 재시작률/재도전율 추적
  - 스킨 코드 구조 개편(visual 우선, trait 슬롯 준비)
- 6개월 방향
  - 기믹 8~12개 확장, 인기 조합 기반 시즌 룰셋 운영
  - 스킨 trait 제한적 랭킹 반영(점수 상세 공개 유지)
  - 경쟁 구조 고도화: 주간 챌린지 + 시즌 이벤트
- 확장 시나리오
  - 이벤트 운영형 라이브 게임 전환(시즌 테마 기믹 묶음)
  - 소셜 확장(친구 간 동일 로드아웃 대결)
  - 플랫폼 확장(모바일 입력 최적화, 크로스 세이브)
